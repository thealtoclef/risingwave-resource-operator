---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: risingwaveschemas.risingwave.risingwavelabs.com
spec:
  group: risingwave.risingwavelabs.com
  names:
    categories:
    - all
    - streaming
    kind: RisingWaveSchema
    listKind: RisingWaveSchemaList
    plural: risingwaveschemas
    shortNames:
    - rws
    singular: risingwaveschema
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .status.schemaCreated
      name: Schema
      type: boolean
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: RisingWaveSchemaSpec defines the desired state of RisingWaveSchema.
            properties:
              connectionRef:
                description: ConnectionRef specifies a direct connection to an externally
                  managed RisingWave frontend.
                properties:
                  credentials:
                    description: |-
                      Credentials for connecting to the RisingWave cluster as an admin.
                      If omitted, connects with username "root" and empty password.
                    properties:
                      password:
                        description: |-
                          Password is the plaintext admin password.
                          Not recommended for production; use passwordSecretRef instead.
                          Defaults to empty string if neither password nor passwordSecretRef is set.
                        type: string
                      passwordSecretRef:
                        description: |-
                          PasswordSecretRef references a Kubernetes Secret containing the admin password.
                          Takes precedence over the password field when both are set.
                        properties:
                          key:
                            description: Key in the secret data containing the password.
                              Defaults to "password".
                            type: string
                          name:
                            description: Name of the secret.
                            type: string
                          namespace:
                            description: Namespace of the secret. Defaults to the
                              same namespace as the RisingWaveUser.
                            type: string
                        required:
                        - name
                        type: object
                      username:
                        default: root
                        description: Username to connect with.
                        type: string
                    type: object
                  host:
                    description: Host is the hostname or IP of the RisingWave frontend
                      service.
                    type: string
                  port:
                    default: 4567
                    description: Port is the PostgreSQL-compatible port of the RisingWave
                      frontend.
                    format: int32
                    maximum: 65535
                    minimum: 1
                    type: integer
                required:
                - host
                type: object
              databaseRef:
                description: DatabaseRef specifies the database this schema belongs
                  to.
                properties:
                  name:
                    description: Name of the database in RisingWave.
                    type: string
                required:
                - name
                type: object
              name:
                description: Schema name in RisingWave. Defaults to metadata.name
                  if empty.
                type: string
              owner:
                description: |-
                  Owner specifies the user who should own this schema.
                  If not specified, the schema owner will be the database owner.
                type: string
            required:
            - connectionRef
            - databaseRef
            type: object
          status:
            description: RisingWaveSchemaStatus defines the observed state of RisingWaveSchema.
            properties:
              conditions:
                description: Conditions represent the latest available observations.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: ObservedGeneration is the generation observed by the
                  controller.
                format: int64
                type: integer
              phase:
                description: Phase is a high-level summary of the lifecycle.
                type: string
              reason:
                description: Reason provides more detail about the current phase.
                type: string
              schemaCreated:
                description: SchemaCreated indicates whether the schema has been created.
                type: boolean
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
